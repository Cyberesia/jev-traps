import { SiteNav } from "@/components/site-nav";
import { ScanLab } from "./scan-lab";
export default function ScanPage() { return <div className="shell"><SiteNav /><main className="submit"><div className="eyebrow">No network calls / no API key</div><h1>Inspect before you ingest.</h1><p className="lead">Paste text or HTML for a deterministic scan in your browser. Nothing you paste is sent to this server or a model provider.</p><ScanLab /><div className="panel"><h2>Need to inspect a URL or an image?</h2><p className="note">URL fetching runs in your local CLI. Image inspection runs in your own process with an explicitly configured vision provider and Jev. This public site does not fetch arbitrary URLs or upload attachments.</p><pre className="code">pnpm scan https://example.com/page</pre><a className="text-link" href="/developers#vision">Configure vision inspection →</a></div></main></div>; }

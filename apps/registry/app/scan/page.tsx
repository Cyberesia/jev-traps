import { SiteNav } from '@/components/site-nav';
import { UrlScan } from './url-scan';
import { ScanLab } from './scan-lab';
export default function ScanPage(){return <div className="shell"><SiteNav/><main className="url-scan-page"><header className="scan-intro"><div className="eyebrow">A second look, before your agent reads</div><h1>A page URL.<br/><span>A clearer picture.</span></h1><p className="lead">Check destination intelligence before retrieval, then inspect permitted public pages for instructions that could redirect an AI agent.</p></header><UrlScan/><section className="scan-boundaries"><div><span>01 / PREFLIGHT</span><h3>Check before contact.</h3><p>A local, time-bound threat feed can stop a reported exact URL before the destination receives a request.</p></div><div><span>02 / RETRIEVE + INSPECT</span><h3>Look for intent.</h3><p>An isolated service retrieves permitted pages without cookies or JavaScript. Static signals and optional Jev judgments inspect the response.</p></div><div><span>03 / DECIDE</span><h3>See the reasoning.</h3><p>Get separate destination and content outcomes. Reporting to the registry remains your separate choice.</p></div></section><details className="scan-advanced"><summary>Developer tools: inspect a text or HTML snippet locally</summary><p className="note">This optional tool runs static checks in your browser. It does not fetch a web page or call Jev.</p><ScanLab/></details></main></div>}

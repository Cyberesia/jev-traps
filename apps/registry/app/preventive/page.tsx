import { SiteNav } from "@/components/site-nav";
export default function Preventive() {return <div className="shell"><SiteNav/><main><header className="dev-header"><div className="eyebrow">Preventive inspection / deployment design</div><h1>Watch what agents<br/><em>actually read.</em></h1><p className="lead">Prioritize exposure to agents, not sensational website categories. A useful watchlist starts with the sources your agents regularly consume.</p></header><div className="preventive-banner"><strong>Design stage · no background scans are running</strong><span>This page describes an operator-controlled scan program. It does not launch requests to the listed sources.</span></div><div className="ecosystem-grid">{[
 ["Documentation & repositories","Agent-readable READMEs, reference pages, package documentation and integration guides."],
 ["Search & retrieval sources","Pages frequently returned to research assistants, support agents and RAG ingestion pipelines."],
 ["Attachments & application frames","Authorized screenshots and synthetic reproductions of email, chat and computer-use content. Private content stays private."],
 ].map(([title,text])=><article className="ecosystem-card" key={title}><div className="eyebrow">Exposure surface</div><h3>{title}</h3><p>{text}</p></article>)}</div><section className="method-section"><h2>A bounded, auditable program.</h2><ol className="journey"><li><b>01</b><div><h3>Approve a finite watchlist</h3><p>The operator selects public, authorized sources and sets per-origin quotas. Agents cannot enroll arbitrary URLs for server-side fetching.</p></div></li><li><b>02</b><div><h3>Fetch in an isolated worker</h3><p>Bind validated public destinations, enforce network egress, check redirects, and bound time, bytes and browser resources. Never run this inside a public Next.js route.</p></div></li><li><b>03</b><div><h3>Inspect meaningful changes</h3><p>Use content hashes to avoid repeat work. Classify changed text/DOM with Jev; inspect pixels separately where needed. Record coverage, model, duration and failures.</p></div></li><li><b>04</b><div><h3>Review before amplifying</h3><p>New signals enter private review. Publish reproducible redacted evidence, not a live list of accused domains. Keep removed or disputed observations traceable.</p></div></li></ol><a className="button" href="https://github.com/cyberesia/jev-traps/blob/main/docs/PREVENTIVE-SCANNING.md">Read the worker specification ↗</a></section></main></div>;}
